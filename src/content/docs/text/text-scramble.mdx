---
title: Text Scramble
description: Text Scramble component is used to create a text animation effect.
slug: /text/text-scramble
published: true
date: 2024-11-23
---

#### Basic Text Scramble
<ComponentCodePreview category="examples" name="text-scramble-basic" hasReTrigger />

#### Text Scramble on Hover
<ComponentCodePreview category="examples" name="text-scramble-custom-trigger" />

#### Text Scramble with Custom Chars
<ComponentCodePreview category="examples" name="text-scramble-custom-chars" hasReTrigger />


## Usage Guide

Install the **clsx** and **tailwind-merge** packages:
```bash
npm install clsx tw-merge
```

Next, add [**cn**](https://github.com/yogendrarana/craftui/blob/master/src/lib/utils.ts) utility function in your project:
```jsx
import { twMerge } from "tailwind-merge";
import { clsx, type ClassValue } from "clsx";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```

Copy and paste the following component source.

<ComponentSource category="components" name="text-scramble" />

## Props

| Prop             | Type      | Description                                 | Default            |
|------------------|-----------|---------------------------------------------|--------------------|
| children         | string    | The text to display                         | "Hello, World!"    |
| duration         | number    | Duration of the scrambling animation (s).   | 3                  |
| speed            | number    | The speed at which scrambling happens (ms). | 50                 |
| trigger          | boolean   | Trigger the animation.                      | true               |
| characterSet     | string    | Custom characters used for scrambling.      | [A-Za-z0-9]        |
| className        | string    | Additional class names for styling.         | _                  |
| as               | string    | HTML tag name to wrap the text inside       | _                  |
| onScrambleEnd    | function  | Function to run after the scrambling ends   | _                  |


