---
published: true
title: Installation
description: Install the necessary packages and set up your project.
---

### Tailwind CSS
Start by setting up Tailwind CSS in your project. 
You can follow the official documentation [here](https://tailwindcss.com/docs/installation) for detailed information.


### Framer Motion
If a component uses **framer-motion** for animations, you’ll need to install it:
```bash filename="terminal" switcher
npm install framer-motion
```

### Utility 
If a component relies on the **cn** utility, install the **clsx** and **tailwind-merge** packages:
```bash
npm install clsx tw-merge
```

Next, add this utility function in your project at **/src/utils/cn.ts**:
```jsx filename="src/utils/cn.ts"
import { twMerge } from "tailwind-merge";
import { clsx, type ClassValue } from "clsx";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```