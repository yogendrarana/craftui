---
title: Button
description: A simple button component.
slug: /components/button
published: true
date: 2024-12-25
---

This one is exension of **shadcn/ui** button component.
If you want to use the original one from **shadcn/ui**, you can visit this link [**here.**](https://ui.shadcn.com/docs/components/button)

#### Buttons with different variants
<ComponentCodePreview category="examples" name="button-variants" />

## Usage guide
Install the **clsx** and **tailwind-merge** packages:
```bash
npm install @radix-ui/react-slot clsx tw-merge class-variance-authority
```

Next, add [**cn**](https://github.com/yogendrarana/craftui/blob/master/src/lib/utils.ts) utility:
```jsx
import { twMerge } from "tailwind-merge";
import { clsx, type ClassValue } from "clsx";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```

Copy and paste the following component source.
<ComponentSource name="button" category="components" />

## Props

| Prop             | Type              | Description                                                         | Default |
| ---------------  | ----------------- | ------------------------------------------------------------------- | ------- |
| children         | ReactNode         | The content to be rendered inside it                                | _       |
| variants         | string            | default, danger, outline, secondary, link, gradient, glow, glass    | default |
| size             | string            | default, sm, lg, icon                                            | default |
| asChild?         | string            | If false, returns button other wise returns children                | false   |
| className?       | string            | Optional class names to style the button component                  | _       |