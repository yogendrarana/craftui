---
title: Marquee
description: A marquee component that scrolls content horizontally.
slug: /components/marquee
published: true
date: 2024-11-11
author: { name: "Yogendra Rana", twitter: "https://twitter.com/yoogendra_rana" }
---

#### Marquee - Basic
The component is using svg icons as marquee content. 
If you want to use these icons you can visit [**svgl.app**](https://www.svgl.app)
<ComponentCodePreview category="examples" name="marquee-basic" />

#### Marquee - Reverse
<ComponentCodePreview category="examples" name="marquee-reverse" />

## Usage Guide

Install the **clsx** and **tailwind-merge** packages:
```bash
npm install clsx tw-merge
```

Next, add [**cn**](https://github.com/yogendrarana/craftui/blob/master/src/lib/utils.ts) utility:
```jsx
import { twMerge } from "tailwind-merge";
import { clsx, type ClassValue } from "clsx";

export function cn(...inputs: ClassValue[]) {
    return twMerge(clsx(inputs));
}
```

Copy and paste the following component source.
<ComponentSource name="marquee" category="core" />

## Props

| Prop         | Type              | Description                                  | Default |
| -----------  | ----------------- | -------------------------------------------- | ------- |
| children     | ReactNode         | Custom class to apply to the component       | _       |
| className    | string            | Custom class to apply to the component.      | _       |
| duration     | number            | Duration of the animation in seconds         | 20      |
| repeat       | number            | Number of times the animation should repeat. | 2       |
| direction    | "left" or "right" | Direction of the animation.                  | "left"  |
| pauseOnHover | boolean           | Whether the animation pauses on hover.       | true    |
| gap          | number            | Space between the animated items in pixels.  | 0       |