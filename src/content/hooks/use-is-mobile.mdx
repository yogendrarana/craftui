---
title: useIsMobile
description: A hook that detects whether the current viewport width is below the mobile breakpoint.
slug: /hooks/use-is-mobile
published: true
date: 2025-01-26T00:00:00.000Z
category: hooks
---

## Installation

```bash
npx craftui add hooks-use-is-mobile
```

## Usage

```tsx
import { useIsMobile } from "@/hooks/use-is-mobile";

function ResponsiveComponent() {
  const isMobile = useIsMobile();

  return (
    <div>
      {isMobile ? (
        <MobileView />
      ) : (
        <DesktopView />
      )}
    </div>
  );
}
```

## API Reference

### useIsMobile

A hook that detects whether the current viewport width is below the mobile breakpoint (768px).

**Returns:** `boolean` - `true` if viewport width is below 768px, `false` otherwise

## Source Code

<ComponentSource name="hooks-use-is-mobile" />
